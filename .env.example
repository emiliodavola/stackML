# StackML - Template de Variables de Entorno
# Copia este archivo como .env y personaliza los valores según tus necesidades

# ================================
# CONFIGURACIÓN DE PUERTOS
# ================================
# Puertos para acceso a cada servicio
MINIO_CONSOLE_PORT=9001
MINIO_PORT=9000
GRAFANA_PORT=3000
PROMETHEUS_PORT=9090
ELASTIC_PORT=9200
MLFLOW_PORT=5000
JUPYTER_PORT=8888
MARIMO_PORT=2718
RSTUDIO_PORT=8787
API_PORT=8000

# ================================
# VERSIONES DE IMÁGENES DOCKER
# ================================
# Especifica las versiones de las imágenes Docker a usar
MINIO_IMAGE=minio/minio:latest
GRAFANA_IMAGE=grafana/grafana:latest
PROMETHEUS_IMAGE=prom/prometheus:latest
ELASTIC_IMAGE=elasticsearch:8.8.0

# ================================
# CREDENCIALES Y AUTENTICACIÓN
# ================================
# ⚠️  IMPORTANTE: Cambia todas las contraseñas antes de usar en producción

# MinIO (Almacenamiento de objetos)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=minioadmin123

# Elasticsearch (Búsqueda y análisis)
ELASTIC_USERNAME=elastic
ELASTIC_PASSWORD=elasticadmin123

# Grafana (Dashboards y visualización)
GF_SECURITY_ADMIN_USER=grafana
GF_SECURITY_ADMIN_PASSWORD=grafanaadmin123

# Jupyter Lab (Notebooks Python)
JUPYTER_PASSWORD=jupyteradmin123

# Marimo (Notebooks interactivos)
MARIMO_PASSWORD=marimoadmin123

# RStudio Server (IDE para R)
RSTUDIO_PASSWORD=rstudio
RSTUDIO_DISABLE_AUTH=false

# ================================
# CONFIGURACIÓN DE DOCKER COMPOSE
# ================================
# Nombre del proyecto para Docker Compose
COMPOSE_PROJECT_NAME=stackml

# ================================
# NOTAS DE CONFIGURACIÓN
# ================================
# - Los servicios básicos (MLflow, MinIO, Marimo, Jupyter, RStudio) se levantan por defecto
# - Los servicios on-demand (Grafana, Prometheus, Elasticsearch, API) requieren --profile on-demand
# - Todos los datos se persisten en la carpeta ./data/
# - Los notebooks se comparten en ./notebooks/work/
# - Para producción, usa contraseñas seguras y considera usar Docker Secrets